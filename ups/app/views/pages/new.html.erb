<p class="actions">
  <%= link_to "back", index_path, :class => "back" %>
</p>
<%= form_for @edit_page, :url => index_path, :html => { :method => :post } do |f| %>
  <%= render 'form', :f => f, :edit_page => @edit_page %>
<% end %>