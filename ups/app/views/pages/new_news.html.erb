<p class="actions">
  <%= link_to "back", news_index_path, :class => "back" %>
</p>
<%= form_for @edit_page, :url => news_index_path, :html => {:method => :post} do |f| %>
  <%= render 'form', :f => f %>
<% end %>