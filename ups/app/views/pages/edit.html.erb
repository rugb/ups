<p class="actions">
  <%= link_to "back", show_path(@edit_page), :class => "back" %>
  <%= link_to "index", index_path, :class => "back" %>
</p>
<%= form_for @edit_page, :url => show_path(@edit_page), :html => { :method => :put } do |f| %>
  <%= render 'form', :f => f, :edit_page => @edit_page %>
<% end %>